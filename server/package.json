{
  "name": "@rayclip/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RayClip authenticated clipboard sync server",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
